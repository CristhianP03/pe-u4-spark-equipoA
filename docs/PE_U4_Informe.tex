\documentclass[11pt,a4paper]{article}

% Preambulo

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[spanish]{babel}
\usepackage{csquotes}
\usepackage[margin=2.5cm]{geometry}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{siunitx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{xcolor}
\usepackage{listings}
\usepackage{caption}
\usepackage{longtable}
\usepackage{array}

\renewcommand{\refname}{Bibliograf\'ia}

\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=blue!50!black,
  citecolor=blue!50!black,
  urlcolor=blue!50!black,
  pdftitle={PE-U4: pandas vs PySpark y comprobacion de la Ley de Amdahl (ACC)},
  pdfauthor={Cristhian Pacheco, Robinson Cando, Ernesto Luna}
}

% --- Estilo de listados de codigo (Python) ---
\lstdefinestyle{pythonstyle}{
  language=Python,
  basicstyle=\ttfamily\footnotesize,
  keywordstyle=\color{blue!60!black}\bfseries,
  commentstyle=\color{gray!70!black}\itshape,
  stringstyle=\color{teal!70!black},
  numbers=left,
  numberstyle=\tiny\color{gray},
  stepnumber=1,
  numbersep=6pt,
  breaklines=true,
  breakatwhitespace=false,
  showstringspaces=false,
  frame=single,
  framerule=0.3pt,
  captionpos=b,
  tabsize=4,
}
\lstset{style=pythonstyle}

% --- Comando para marcar campos institucionales que el equipo debe completar ---
\newcommand{\completar}[1]{\colorbox{yellow!60}{\textbf{[COMPLETAR: #1]}}}

\title{PE-U4: pandas vs PySpark y comprobacion experimental de la Ley de Amdahl}
\author{}
\date{}

\begin{document}

% PORTADA

\begin{titlepage}
\centering
{\large UNIVERSIDAD T\'ECNICA ESTATAL DE QUEVEDO\par}
{\large Facultad de Ciencias de la Computaci\'on\par}
{\large Carrera de Ingenier\'ia de Software\par}

\vspace{1.2cm}
{\Large\bfseries PE-U4: Procesamiento Distribuido con Apache Spark\\[2pt]
Comprobaci\'on Experimental de la Ley de Amdahl\\[2pt]
sobre un Pipeline de Tickets de Soporte T\'ecnico ISP (ACC)\par}

\vspace{0.8cm}
\begin{tabular}{@{}ll@{}}
\textbf{C\'odigo de actividad:} & GA-SUM-05 / PE-U4 \\
\textbf{Asignatura:} & Aplicaciones Distribuidas (ISR-701) \\
\textbf{Unidad:} & Unidad 4 \\
\textbf{Nivel / Paralelo:} & S\'eptimo de Ingenier\'ia de Software ``A'' \\
\textbf{Docente:} & Gleiston C. Guerrero-Ulloa, M.Sc. \\
\textbf{Per\'iodo acad\'emico:} & 2026--2027 PPA \\
\end{tabular}

\vspace{0.8cm}
\textbf{PFC de referencia:} ACC --- Sistema de Gesti\'on de Soporte T\'ecnico ISP

\vspace{0.6cm}
{\large\textbf{Integrantes}\par}
\vspace{0.2cm}
\resizebox{\textwidth}{!}{%
\begin{tabular}{@{}lll@{}}
\toprule
\textbf{Nombre} & \textbf{PFC de origen} & \textbf{Rol asumido en esta pr\'actica} \\
\midrule
Cristhian Daniel Pacheco C\'ardenas & ACC & Implementaci\'on PySpark, protocolo de medici\'on y escalado de T3 \\
Robinson Rodrigo Cando Moreno & ACC & An\'alisis de Amdahl, equivalencia y umbral de rentabilidad \\
Ernesto Gregory Luna Mora & BCEL & Repositorio, reproducibilidad, figuras y verificaci\'on final \\
\bottomrule
\end{tabular}%
}

\vspace{0.8cm}
\begin{minipage}{0.92\textwidth}
\textbf{Justificaci\'on t\'ecnica.} El dominio ACC (Sistema de Gesti\'on de Soporte
T\'ecnico ISP) opera sobre un flujo continuo de tickets de soporte --cortes de
servicio, reclamos de velocidad, fallas de equipo, solicitudes de instalaci\'on
y consultas de facturaci\'on-- generado las 24 horas por una base de clientes
activa. Incluso un proveedor de tama\~no medio, con algunos miles de clientes,
acumula en pocos meses de operaci\'on cientos de miles de registros de
tickets, cada uno con m\'ultiples atributos temporales, categ\'oricos y de
estado. A esa escala, el an\'alisis explorat\'orio, los reportes gerenciales
recurrentes y las agregaciones por zona o categor\'ia dejan de ser viables con
herramientas de una sola m\'aquina: el tiempo de c\'omputo crece de forma que
compromete la oportunidad de la decisi\'on operativa. Este trabajo usa
exactamente ese volumen (600\,000 tickets sint\'eticos, semilla fija) para
comprobar, con datos propios, si un motor distribuido como Apache Spark ofrece
una ventaja medible frente a pandas. La decisi\'on de negocio concreta que se
apoyar\'ia en este pipeline es identificar qu\'e categor\'ias de incidencia
concentran m\'as tickets cr\'iticos por zona operativa, para decidir d\'onde
reforzar personal t\'ecnico de campo o priorizar mantenimiento de
infraestructura de red --una decisi\'on operativa medible, no una mejora
gen\'erica del servicio.
\end{minipage}

\vfill
{\small Repositorio: \url{https://github.com/CristhianP03/pe-u4-spark-equipoA}\par}
\end{titlepage}

\tableofcontents
\newpage

\section*{Resumen}
\addcontentsline{toc}{section}{Resumen}

La Ley de Amdahl (1967) establece el l\'imite te\'orico de aceleraci\'on que
puede obtenerse al paralelizar un programa cuya fracci\'on serial es fija, y
sigue siendo el marco de referencia para evaluar si motores modernos de
procesamiento distribuido, como Apache Spark, cumplen su promesa de
escalabilidad sobre cargas de trabajo reales. Este informe comprueba
experimentalmente dicha ley aplicando un pipeline de cinco transformaciones
(filtrado, agrupaci\'on, join, generaci\'on de variables derivadas y top-N)
sobre un dataset sint\'etico determinista de 600\,000 tickets de soporte
t\'ecnico ISP del dominio ACC, implementado en paralelo en pandas y en
PySpark~3.5.3, bajo un protocolo de medici\'on riguroso (calentamiento
descartado, cinco repeticiones cronometradas con \texttt{time.perf\_counter},
estad\'istico mediana) y con escalado de la transformaci\'on de join (T3) en
$N=1,2,4$ executors. Los resultados muestran una aceleraci\'on de PySpark
frente a pandas de entre 1.43$\times$ y 2.56$\times$ seg\'un la
transformaci\'on, con equivalencia num\'erica exacta entre ambos motores en
las cinco transformaciones (cardinalidad, columnas, sumas, medias y claves de
agrupaci\'on). El an\'alisis de Amdahl sobre T3 estima una fracci\'on
paralelizable observada de apenas 17.3\,\%, lo que fija un techo te\'orico de
aceleraci\'on de 1.21$\times$ y explica por qu\'e el rendimiento se satura ya
con $N=2$ executors --un hallazgo consistente con el \emph{shuffle} del join
como cuello de botella dominante, m\'as que con una limitaci\'on
algor\'itmica intr\'inseca. Estos resultados tienen una implicaci\'on directa
para ACC: la migraci\'on a Spark se justifica por el volumen de datos que hay
que procesar, no por el paralelismo bruto de hardware adicional.

\newpage

\section{Introducci\'on}

El crecimiento sostenido del volumen de datos operativos en sistemas
empresariales ha convertido el procesamiento distribuido en una necesidad
pr\'actica m\'as que en una opci\'on de dise\~no: cuando un conjunto de datos
supera lo que una sola m\'aquina puede procesar en un tiempo aceptable, la
\'unica v\'ia para mantener la oportunidad de las decisiones basadas en esos
datos es repartir el c\'omputo entre varias unidades de procesamiento
\cite{dean2008,zaharia2016}.

Sin embargo, repartir el trabajo entre m\'as procesadores no garantiza una
aceleraci\'on proporcional. La Ley de Amdahl, formulada en 1967, es el marco
cl\'asico que cuantifica ese l\'imite: toda tarea computacional tiene, en la
pr\'actica, una fracci\'on que no puede paralelizarse --ya sea por
dependencias de datos, coordinaci\'on entre nodos o simple l\'ogica
secuencial-- y esa fracci\'on fija un techo de aceleraci\'on que ning\'un
n\'umero adicional de procesadores puede superar. M\'as de cinco d\'ecadas
despu\'es de su formulaci\'on, la vigencia de esta ley sigue siendo objeto de
verificaci\'on emp\'irica cuando se aplica a motores modernos de
procesamiento distribuido como Apache Spark, cuyo modelo de ejecuci\'on en
memoria y evaluaci\'on perezosa difiere sustancialmente de los sistemas para
los que Amdahl escribi\'o originalmente su argumento.

El objetivo de este trabajo es comprobar experimentalmente la Ley de Amdahl
--y su complemento, la Ley de Gustafson-Barsis-- aplicando un pipeline real
de cinco transformaciones de datos sobre un dataset sint\'etico de 600\,000
tickets de soporte t\'ecnico ISP, correspondiente al dominio ACC (Sistema de
Gesti\'on de Soporte T\'ecnico ISP), implementado en paralelo en pandas
(l\'inea base de una sola m\'aquina) y en PySpark (motor distribuido), con
escalado controlado de una de las transformaciones en $N=1,2,4$ executors.

El documento se organiza de la siguiente manera: la Secci\'on~\ref{sec:fundamento}
desarrolla el fundamento te\'orico del paralelismo, MapReduce/Hadoop, la
arquitectura de Spark y los modelos de computaci\'on en la nube; la
Secci\'on~\ref{sec:procedimiento} documenta el procedimiento aplicado
(dataset, configuraci\'on de Spark, implementaci\'on y verificaci\'on de
equivalencia); la Secci\'on~\ref{sec:resultados} presenta los resultados
cuantitativos; la Secci\'on~\ref{sec:analisis} desarrolla el an\'alisis
cr\'itico de la fracci\'on serial observada y el umbral de rentabilidad; la
Secci\'on~\ref{sec:preguntas} responde las preguntas de an\'alisis del Anexo
A; y el documento cierra con las conclusiones individuales del equipo y la
declaraci\'on de uso de inteligencia artificial generativa.

\section{Fundamento Te\'orico}
\label{sec:fundamento}

\subsection{Fundamentos de paralelismo y Ley de Amdahl}
\label{sec:fundamento-amdahl}

El paralelismo de c\'omputo se clasifica, de forma general, en dos
estrategias complementarias. El \textbf{paralelismo de datos} consiste en
dividir un mismo conjunto de datos en fragmentos independientes y aplicar
\emph{la misma} operaci\'on a cada fragmento de forma simult\'anea en
distintas unidades de procesamiento; es el modelo que sigue Apache Spark al
particionar un DataFrame entre executors, donde cada partici\'on se procesa
con id\'entica l\'ogica de transformaci\'on \cite{zaharia2012,chambers2018}.
El \textbf{paralelismo de tareas}, en cambio, consiste en ejecutar
simult\'aneamente operaciones \emph{distintas} entre s\'i, cada una
potencialmente sobre datos diferentes, coordinadas para producir un resultado
com\'un; es el modelo m\'as cercano a las fases Map y Reduce como unidades de
trabajo diferenciadas dentro de un mismo job \cite{dean2008,white2015}. En el
pipeline de este proyecto, T1--T5 se ejecutan como tareas independientes
entre s\'i (paralelismo de tareas a nivel de pipeline), mientras que, dentro
de cada transformaci\'on, el procesamiento de las 600\,000 filas se reparte
por partici\'on entre executors (paralelismo de datos a nivel de
transformaci\'on).

\paragraph{Derivaci\'on de la Ley de Amdahl.}
Sea $T$ el tiempo total de ejecuci\'on de un programa en una sola unidad de
procesamiento, normalizado a $T=1$. Este tiempo se compone de dos fracciones
complementarias: una fracci\'on serial $(1-p)$, correspondiente a las
instrucciones que deben ejecutarse estrictamente en orden (por dependencias
de datos, inicializaci\'on, o agregaci\'on final de resultados) y que ninguna
cantidad de procesadores adicionales puede acelerar; y una fracci\'on
paralelizable $p$, que s\'i puede repartirse entre $N$ unidades de
procesamiento sin p\'erdida de eficiencia ideal. Bajo el supuesto de que la
fracci\'on paralelizable se reparte de forma perfectamente uniforme entre las
$N$ unidades, el tiempo que tarda esa porci\'on se reduce a $p/N$, mientras
que el tiempo de la fracci\'on serial permanece constante en $(1-p)$, porque
por definici\'on no se distribuye. El tiempo total con $N$ procesadores es
entonces $(1-p)+p/N$, y el \emph{speedup} $S(N)$ --la raz\'on entre el tiempo
original y el tiempo paralelizado-- resulta de dividir el tiempo original
($T=1$) entre ese tiempo reducido, lo que da la ecuaci\'on~\eqref{eq:amdahl},
conocida como la Ley de Amdahl \cite{amdahl1967,gustafson1988}:
\begin{equation}
S(N) = \frac{1}{(1-p) + \dfrac{p}{N}}
\label{eq:amdahl}
\end{equation}

\paragraph{Speedup m\'aximo te\'orico.}
Cuando $N$ crece sin l\'imite, el t\'ermino $p/N$ tiende a 0 y el speedup se
estabiliza en el valor dado por la ecuaci\'on~\eqref{eq:smax}: el speedup
m\'aximo te\'orico depende exclusivamente de la fracci\'on serial, no del
n\'umero de procesadores disponibles.
\begin{equation}
S_{\max} = \lim_{N\to\infty} S(N) = \frac{1}{1-p}
\label{eq:smax}
\end{equation}
La implicaci\'on pr\'actica es contundente: si el 10\,\% de un programa es
estrictamente serial ($p=0.9$), el speedup m\'aximo posible es
$1/(1-0.9)=1/0.1=10$, sin importar si se usan 100 o 10\,000 procesadores
--agregar m\'as recursos de c\'omputo m\'as all\'a de cierto punto deja de
tener retorno \cite{amdahl1967,gustafson1988}.

\paragraph{Eficiencia del paralelismo.}
La eficiencia, definida en la ecuaci\'on~\eqref{eq:eficiencia}, mide qu\'e
fracci\'on del beneficio ideal (speedup lineal, $E=1$) se aprovecha realmente
con cada unidad de procesamiento adicional.
\begin{equation}
E(N) = \frac{S(N)}{N}
\label{eq:eficiencia}
\end{equation}
Un speedup absoluto alto obtenido con un n\'umero grande de procesadores
puede, pese a todo, tener una eficiencia baja: es la se\~nal cuantitativa de
que se est\'a entrando en la zona de rendimientos decrecientes que predice la
propia Ley de Amdahl \cite{amdahl1967,chambers2018}.

\paragraph{Ley de Gustafson-Barsis.}
La Ley de Amdahl asume un supuesto que rara vez se sostiene en sistemas de
Big Data: que el tama\~no del problema es fijo y lo \'unico que cambia es el
n\'umero de procesadores disponibles para resolverlo m\'as r\'apido.
Gustafson \cite{gustafson1988} invierte ese supuesto en 1988: en la
pr\'actica, cuando una organizaci\'on agrega m\'as capacidad de c\'omputo no
es porque quiera resolver exactamente el mismo problema en menos tiempo,
sino porque quiere procesar m\'as datos en el mismo tiempo disponible. Bajo
ese supuesto de carga de trabajo escalable, el speedup observado se modela
con la ecuaci\'on~\eqref{eq:gustafson}, conocida como Ley de
Gustafson-Barsis:
\begin{equation}
S_G(N) = N - (1-p)(N-1)
\label{eq:gustafson}
\end{equation}
Esta distinci\'on es particularmente relevante para sistemas como Apache
Spark: cuando un equipo de datos escala un cl\'uster, t\'ipicamente no es
para terminar el mismo reporte m\'as r\'apido, sino para poder procesar, en
la misma ventana de tiempo, un volumen de tickets, transacciones o eventos
que ha crecido \cite{gustafson1988,zaharia2016}. Por esta raz\'on, las leyes
de Amdahl y Gustafson-Barsis no son contradictorias sino complementarias: la
primera acota el beneficio de paralelizar un problema de tama\~no fijo, y la
segunda describe el escenario --mucho m\'as com\'un en la pr\'actica de Big
Data-- de un problema cuyo tama\~no crece junto con los recursos.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.8\textwidth]{../resultados/figuras/fig0_amdahl_teorico.png}
\caption{Curvas te\'oricas de speedup $S(N)$ seg\'un la ecuaci\'on~\eqref{eq:amdahl} para cuatro valores de la fracci\'on paralelizable ($p=0.50,\,0.75,\,0.90,\,0.95$).}
\label{fig:teorico-amdahl}
\end{figure}

La Figura~\ref{fig:teorico-amdahl} ilustra las curvas de speedup predichas
por la ecuaci\'on~\eqref{eq:amdahl}, mostrando c\'omo el techo de
aceleraci\'on se desplaza dr\'asticamente seg\'un qu\'e tan paralelizable
sea, en t\'erminos algor\'itmicos, el problema: incluso con $p=0.95$ (un
programa con solo 5\,\% de c\'odigo estrictamente serial), el speedup se
satura por debajo de 20$\times$, mientras que con $p=0.50$ el techo es de
apenas 2$\times$.

\paragraph{Fracci\'on serial observada (forma inversa).}
Despejando $p$ de la ecuaci\'on~\eqref{eq:amdahl} en funci\'on de un speedup
y un $N$ observados experimentalmente, se obtiene la forma inversa de la
ecuaci\'on~\eqref{eq:fobs}, que permite estimar --a partir de mediciones
reales, no de una declaraci\'on de dise\~no-- qu\'e fracci\'on del tiempo
total se comport\'o como si fuera serial en una ejecuci\'on concreta:
\begin{equation}
f_{\mathrm{obs}}(N) = \frac{\dfrac{1}{S(N)} - \dfrac{1}{N}}{1 - \dfrac{1}{N}}
\label{eq:fobs}
\end{equation}
Es indispensable, sin embargo, no confundir esta magnitud despejada con la
fracci\'on serial algor\'itmica del programa: $f_{\mathrm{obs}}(N)$ no
distingue entre instrucciones que son intr\'insecamente secuenciales (por
dise\~no del algoritmo) y tiempo perdido por sobrecarga de planificaci\'on de
tareas, serializaci\'on de datos entre procesos o --de forma especialmente
relevante en Spark-- el \emph{shuffle} necesario para redistribuir datos
entre executors en operaciones como el join o el \texttt{groupBy}. Confundir
la fracci\'on serial observada con la fracci\'on serial algor\'itmica es un
error conceptual expl\'icitamente penalizado en el criterio 2.2 de esta
r\'ubrica, y se retoma de forma aplicada, con los datos reales del
experimento, en la Secci\'on~\ref{sec:analisis}.

\paragraph{Conexi\'on con el dominio ACC.}
En el pipeline de tickets de soporte de ACC, esta distinci\'on tiene una
traducci\'on directa: T1 (filtrado) es una operaci\'on de paralelismo de
datos casi puro --cada fila se eval\'ua de forma completamente independiente
contra la m\'ascara de condiciones, sin que el resultado de un ticket
dependa del de otro-- por lo que se espera que su fracci\'on serial observada
sea baja. T3 (join), en cambio, requiere redistribuir f\'isicamente las
filas de \texttt{tickets} y \texttt{agentes} entre executors seg\'un la
clave \texttt{agente\_id} antes de poder emparejarlas (\emph{shuffle}), una
operaci\'on de comunicaci\'on entre nodos que Amdahl, en su formulaci\'on
cl\'asica de 1967, no distingue de la fracci\'on serial pura del algoritmo.
Por esta raz\'on, T3 es la transformaci\'on elegida en este experimento para
el escalado con $N=1,2,4$ executors: es la que mejor expone, con datos
reales, la diferencia entre paralelismo te\'orico y paralelismo efectivo.

\subsection{Paradigma MapReduce y Apache Hadoop}
\label{sec:fundamento-mapreduce}

El modelo de programaci\'on MapReduce, formalizado por Dean y Ghemawat en
Google y publicado en su versi\'on ampliamente citada en 2008
\cite{dean2008}, surgi\'o de una motivaci\'on concreta: permitir que
ingenieros sin experiencia en sistemas distribuidos pudieran procesar
vol\'umenes de datos del orden de terabytes o petabytes sobre cl\'usteres de
hardware com\'un (\emph{commodity hardware}), abstrayendo de la l\'ogica de
la aplicaci\'on los detalles de distribuci\'on de datos, paralelizaci\'on,
tolerancia a fallos y balanceo de carga \cite{dean2008,white2015}.

El modelo se estructura en dos funciones puras que el programador debe
implementar. La funci\'on \textbf{Map} recibe pares clave-valor de entrada y
produce, para cada uno, cero o m\'as pares clave-valor intermedios. La
funci\'on \textbf{Reduce} recibe, para cada clave intermedia distinta, la
lista completa de valores asociados a esa clave (ya agrupados por el propio
framework) y produce el resultado final agregado para esa clave
\cite{dean2008}. Un ejemplo aplicado al dominio ACC ilustra el patr\'on con
claridad: para contar tickets por categor\'ia de incidencia, Map emitir\'ia,
por cada ticket procesado, el par (\texttt{categoria}, 1); Reduce
recibir\'ia, para cada categor\'ia, la lista completa de unos emitidos y
devolver\'ia su suma --exactamente la agregaci\'on de conteo que en este
proyecto se implementa como T2 mediante \texttt{groupBy}.

La ejecuci\'on completa de un job de MapReduce atraviesa cinco fases
\cite{dean2008,white2015}: \emph{split}, donde el dataset de entrada se
divide en fragmentos independientes que pueden procesarse en paralelo;
\emph{map}, donde cada fragmento se procesa de forma independiente y en
paralelo por una tarea Map; \emph{shuffle}, donde los pares intermedios se
redistribuyen entre los nodos seg\'un su clave, de modo que todos los valores
de una misma clave terminen en el mismo nodo; \emph{sort}, donde los valores
dentro de cada partici\'on se ordenan por clave antes de agruparlos; y
\emph{reduce}, donde se ejecuta la funci\'on de agregaci\'on final sobre cada
grupo de clave-valores.

La implementaci\'on de referencia de este modelo, Apache Hadoop, se apoya en
dos componentes arquitect\'onicos: HDFS (\emph{Hadoop Distributed File
System}), un sistema de archivos distribuido que replica cada bloque de
datos en varios nodos (t\'ipicamente tres copias) para garantizar tolerancia
a fallos sin depender de hardware especializado; y YARN (\emph{Yet Another
Resource Negotiator}), el gestor de recursos del cl\'uster que asigna
memoria y c\'omputo a las tareas Map y Reduce en ejecuci\'on
\cite{white2015,dean2008}. Esta arquitectura ofrece dos ventajas centrales:
escalabilidad horizontal con hardware econ\'omico (agregar m\'as nodos
aumenta linealmente la capacidad de almacenamiento y c\'omputo) y tolerancia
a fallos autom\'atica mediante replicaci\'on, sin intervenci\'on del
programador \cite{white2015}.

Sin embargo, el propio dise\~no de MapReduce impone una limitaci\'on
estructural relevante para cargas de trabajo iterativas o de consulta
repetida sobre el mismo conjunto de datos: cada job MapReduce lee sus datos
de entrada desde disco (HDFS) al iniciar y escribe su resultado de vuelta a
disco al terminar, incluso si el siguiente job en una cadena de
procesamiento va a volver a leer exactamente esos mismos datos. En
algoritmos iterativos --consultas repetidas, algoritmos de aprendizaje
autom\'atico que requieren m\'ultiples pasadas sobre el mismo dataset-- este
patr\'on genera una sobrecarga de entrada/salida a disco que se repite en
cada iteraci\'on, en vez de aprovechar que los datos ya est\'an disponibles
en memoria desde la iteraci\'on anterior. Esta limitaci\'on es, precisamente,
el problema central que Apache Spark fue dise\~nado para resolver mediante
su modelo de datos en memoria, desarrollado en la Secci\'on~\ref{sec:fundamento-spark}.

Por tratarse de una comparaci\'on de rendimiento entre motores, cualquier
cifra concreta de cu\'antas veces m\'as r\'apido es un motor respecto al otro
debe respaldarse con evidencia emp\'irica publicada y no con material
promocional de proveedores. El estudio de referencia usado en este informe
para esa comparaci\'on es el benchmark HiBench de Ahmed et al.
\cite{ahmed2020}, retomado con datos propios en la Secci\'on~\ref{sec:fundamento-spark}.

\subsection{Apache Spark: arquitectura y modelo de programaci\'on}
\label{sec:fundamento-spark}

Apache Spark organiza su ejecuci\'on en tres componentes
\cite{zaharia2016,chambers2018}: el \textbf{driver}, el proceso donde se
ejecuta el programa principal del usuario, se construye el grafo de
transformaciones y se coordina la ejecuci\'on distribuida; el \textbf{cluster
manager} (YARN, Kubernetes, Mesos, o el modo \texttt{local[N]} usado en este
proyecto), responsable de asignar recursos f\'isicos a la aplicaci\'on; y los
\textbf{executors}, procesos que se ejecutan en los nodos de trabajo, corren
las tareas asignadas y almacenan particiones de datos en memoria o disco
seg\'un se requiera. En el modo \texttt{local[N]} usado en Colab para este
experimento, todos estos roles conviven en una sola m\'aquina, y $N$
determina el n\'umero de hilos que simulan executors --una simplificaci\'on
necesaria para un entorno educativo de una sola m\'aquina, y una limitaci\'on
que se documenta expl\'icitamente en la Secci\'on~\ref{sec:procedimiento}.

La abstracci\'on fundamental de datos de Spark es el RDD (\emph{Resilient
Distributed Dataset}), introducido por Zaharia et al. \cite{zaharia2012}: una
colecci\'on distribuida e inmutable de objetos, particionada entre los nodos
del cl\'uster, sobre la que solo pueden aplicarse transformaciones
deterministas. La propiedad clave de los RDD para la tolerancia a fallos es
su \emph{lineage} (linaje): en vez de replicar f\'isicamente los datos como
hace HDFS, Spark registra la secuencia de transformaciones que produjo cada
RDD a partir de los datos originales. Si una partici\'on se pierde por el
fallo de un nodo, Spark puede reconstruirla recalculando \'unicamente esa
porci\'on del linaje, sin necesidad de leer una copia f\'isica de respaldo
\cite{zaharia2012}. Esta es una diferencia conceptual central frente al
modelo de HDFS descrito en la Secci\'on~\ref{sec:fundamento-mapreduce}.

Sobre los RDD, Spark ofrece capas de abstracci\'on de m\'as alto nivel
--DataFrames y Datasets-- que a\~naden un esquema estructurado (columnas con
nombre y tipo declarado), de forma an\'aloga a una tabla relacional
\cite{zaharia2016,chambers2018}. Este esquema expl\'icito es lo que permite
al motor aplicar optimizaciones autom\'aticas que sobre un RDD sin tipado
estructurado no ser\'ian posibles de forma gen\'erica. Todas las
transformaciones T1--T5 de este proyecto est\'an implementadas sobre la API
de DataFrames, tanto en su versi\'on PySpark como en su equivalente
conceptual de pandas.

Un rasgo de dise\~no central de Spark, y que tuvo una implicaci\'on
metodol\'ogica directa en este experimento, es la \textbf{evaluaci\'on
perezosa} (\emph{lazy evaluation}): las transformaciones (\texttt{filter},
\texttt{groupBy}, \texttt{join}, \texttt{withColumn}) no se ejecutan en el
momento en que se invocan en el c\'odigo; Spark las acumula en un plan
l\'ogico y, sobre \'el, construye un plan f\'isico optimizado, pero no
materializa ning\'un c\'omputo hasta que se invoca una \textbf{acci\'on}
(\texttt{count()}, \texttt{collect()}, o una escritura a disco como
\texttt{write.csv()}) \cite{zaharia2016,chambers2018}. Esta caracter\'istica
es la que obliga, t\'ecnicamente, a que el protocolo de medici\'on de este
proyecto (Secci\'on~\ref{sec:procedimiento}) fuerce siempre una escritura a
disco al final de cada transformaci\'on medida: sin una acci\'on expl\'icita,
un cron\'ometro colocado alrededor del c\'odigo de transformaci\'on
medir\'ia solo el tiempo de construir el plan de ejecuci\'on (microsegundos),
no el trabajo real de leer, transformar y persistir 600\,000 filas.

El plan f\'isico que Spark construye se representa como un DAG (grafo
ac\'iclico dirigido) de \emph{stages}, y cada stage se subdivide en
\emph{tasks} que se ejecutan en paralelo sobre las particiones de datos,
t\'ipicamente uno por n\'ucleo/executor disponible \cite{zaharia2016}. Este
es exactamente el nivel de detalle que se observa en la captura de la Spark
UI incluida como evidencia en la Secci\'on~\ref{sec:resultados}
(Figura~\ref{fig:sparkui}), que muestra el DAG real de la transformaci\'on T3
(join) ejecut\'andose sobre el cl\'uster local de Colab, incluyendo la etapa
de \emph{shuffle} discutida en la Secci\'on~\ref{sec:analisis}.

Como comparaci\'on de rendimiento frente a MapReduce: el procesamiento en
memoria de Spark evita, en cargas de trabajo que reutilizan datos entre
operaciones, las escrituras a disco entre etapas que penalizan a MapReduce en
flujos iterativos (Secci\'on~\ref{sec:fundamento-mapreduce}); el benchmark
HiBench de Ahmed et al. \cite{ahmed2020} documenta emp\'iricamente esta
ventaja sobre cargas de trabajo comparables, resultado que se contrasta con
los datos propios de este proyecto en la Secci\'on~\ref{sec:resultados}.

Como complemento recomendado, vale precisar el papel del optimizador
\textbf{Catalyst}: antes de ejecutar un plan sobre DataFrames, Spark lo
reescribe autom\'aticamente --por ejemplo, reordenando los filtros para que
se apliquen lo antes posible y reduzcan cuanto antes el volumen de datos que
atraviesa las etapas siguientes-- lo que explica por qu\'e las
transformaciones sobre DataFrames no necesariamente se ejecutan en el orden
textual en que fueron escritas en el c\'odigo \cite{zaharia2016,chambers2018}.

\subsection{Computaci\'on en la nube: modelos y proveedores}
\label{sec:fundamento-nube}

El Instituto Nacional de Est\'andares y Tecnolog\'ia de Estados Unidos (NIST)
define el c\'omputo en la nube, en su publicaci\'on de referencia SP 800-145,
como un modelo que habilita acceso bajo demanda, conveniente y a trav\'es de
la red, a un conjunto compartido de recursos de c\'omputo configurables
(redes, servidores, almacenamiento, aplicaciones y servicios) que pueden
aprovisionarse y liberarse con un esfuerzo de gesti\'on m\'inimo
\cite{mell2011}. Esta definici\'on formaliza cuatro modelos de servicio,
organizados por el nivel de abstracci\'on que ofrecen al usuario:
\textbf{IaaS} (\emph{Infrastructure as a Service}), que expone m\'aquinas
virtuales, redes y almacenamiento en bruto; \textbf{PaaS} (\emph{Platform as
a Service}), que entrega un entorno de ejecuci\'on gestionado sobre el cual
el usuario despliega su propia aplicaci\'on sin administrar la
infraestructura subyacente; \textbf{SaaS} (\emph{Software as a Service}),
donde el usuario consume una aplicaci\'on completa a trav\'es de la red; y
\textbf{FaaS} (\emph{Function as a Service}), un modelo de c\'omputo
\emph{serverless} en el que el usuario despliega funciones individuales que
se ejecutan en respuesta a eventos, sin aprovisionar servidores de forma
expl\'icita \cite{mell2011}.

El mismo documento del NIST identifica cinco caracter\'isticas esenciales que
distinguen al c\'omputo en la nube de la infraestructura tradicional
\cite{mell2011}: \emph{autoservicio bajo demanda} (el usuario aprovisiona
recursos de c\'omputo autom\'aticamente, sin intervenci\'on humana del
proveedor); \emph{acceso amplio a la red} (los recursos est\'an disponibles a
trav\'es de mecanismos est\'andar, accesibles desde plataformas
heterog\'eneas); \emph{agrupaci\'on de recursos} (\emph{pooling}), donde los
recursos f\'isicos del proveedor sirven a m\'ultiples clientes bajo un modelo
multi-inquilino, asignados din\'amicamente seg\'un demanda; \emph{elasticidad
r\'apida}, la capacidad de aprovisionar y liberar recursos de forma
pr\'acticamente instant\'anea para adaptarse a la carga; y \emph{servicio
medido}, donde el uso de recursos se monitorea, controla y factura de forma
transparente para el proveedor y el consumidor.

Los tres proveedores dominantes de nube p\'ublica --Amazon Web Services,
Microsoft Azure y Google Cloud Platform-- ofrecen cada uno un servicio
gestionado espec\'ificamente para ejecutar Apache Spark a escala de
producci\'on: Amazon EMR, Azure HDInsight (o su sucesor Azure Databricks) y
Google Cloud Dataproc, respectivamente \cite{chambers2018,zaharia2016}. Estos
servicios abstraen exactamente la parte de la gesti\'on de infraestructura
que, en este proyecto, se simplific\'o artificialmente con fines educativos
usando el modo \texttt{local[N]} de Google Colab: el aprovisionamiento de
nodos, el escalado autom\'atico del cl\'uster seg\'un la carga, el parcheo de
seguridad del sistema operativo y la configuraci\'on de red entre executors.
En un despliegue real de un sistema de soporte t\'ecnico ISP como ACC,
ser\'ia uno de estos tres servicios gestionados --no un cl\'uster local
simulado-- el que sostendr\'ia el pipeline de procesamiento en producci\'on.

Finalmente, dos patrones arquitect\'onicos son relevantes para decidir c\'omo
evolucionar\'ia este pipeline si los datos de tickets llegaran en tiempo real
en vez de en lote, como se discute en la Pregunta~3 del Anexo A
(Secci\'on~\ref{sec:preguntas}): la arquitectura \textbf{Lambda} combina una
capa de procesamiento por lotes (\emph{batch}, que recalcula vistas exactas
sobre el hist\'orico completo de datos, con alta latencia pero exactitud
garantizada) con una capa de velocidad (\emph{speed layer}, que procesa el
flujo reciente de eventos con baja latencia pero resultados aproximados),
unificadas en una capa de servicio que combina ambas vistas para responder
consultas; la arquitectura \textbf{Kappa}, por su parte, simplifica este
esquema eliminando la capa de lotes y tratando tambi\'en los datos
hist\'oricos como un flujo reproducible sobre el mismo motor de streaming,
evitando as\'i mantener dos rutas de c\'odigo distintas para el mismo
c\'omputo \cite{zaharia2016,mell2011}.

Si el pipeline de tickets de soporte de ACC se llevara a producci\'on, la
elecci\'on entre procesamiento por lotes (el enfoque de esta pr\'actica) y
streaming depender\'ia directamente de la urgencia de la decisi\'on de
negocio que dependa de \'el: un reporte gerencial semanal de tickets por
categor\'ia admite perfectamente un procesamiento batch nocturno, pero una
alerta de corte masivo de servicio --donde cientos de tickets de la misma
zona llegan en minutos-- exige una arquitectura de streaming (Lambda o Kappa)
capaz de disparar una respuesta operativa casi en tiempo real.

\section{Procedimiento Aplicado}
\label{sec:procedimiento}

\subsection{Entorno y conjunto de datos}
\label{sec:dataset}

No existe un dataset real p\'ublico de tickets de soporte ISP con al menos
500\,000 registros (verificado por el equipo). La gu\'ia de la asignatura
permite expl\'icitamente el uso de datos sint\'eticos generados con
\texttt{Faker} y semilla fija, declarado en el informe. Por ello, el equipo
gener\'o de forma determinista (\texttt{src/generar\_dataset\_acc.py},
semilla \texttt{42}) dos tablas relacionadas mediante la clave for\'anea
\texttt{tickets.agente\_id} $\rightarrow$ \texttt{agentes.agente\_id}, dise\~no
que hace gen\'uino el \emph{join} de T3 (no un \emph{self-join} sobre el
propio dataset). El esquema (categor\'ias, estados, prioridades con su SLA en
horas, canales y zonas) est\'a anclado al dominio real del PFC ACC. La
Tabla~\ref{tab:dataset} resume los metadatos exigidos por el criterio 1.1.

\begin{table}[htbp]
\centering
\caption{Metadatos del dataset sint\'etico de tickets ISP (ACC).}
\label{tab:dataset}
\begin{tabular}{@{}p{3.3cm}p{9.5cm}@{}}
\toprule
\textbf{Campo} & \textbf{Valor} \\
\midrule
Fuente & Sint\'etico (Faker \texttt{es\_MX} + numpy/pandas, semilla fija 42) \\
URL (generaci\'on) & \texttt{src/generar\_dataset\_acc.py} (repositorio \texttt{Soporte-Tecnico-ACC}) \\
Licencia & Datos generados por el propio equipo; c\'odigo bajo licencia MIT (\texttt{LICENSE}) \\
Fecha de generaci\'on (UTC) & 2026-08-07T04:50:09+00:00 \\
Registros & 600\,000 tickets / 300 agentes \\
Columnas & 15 (\texttt{tickets.csv}) / 7 (\texttt{agentes.csv}) \\
Tama\~no en disco & 75.8\,MB (\texttt{tickets.csv}) / $<$0.1\,MB (\texttt{agentes.csv}) \\
\bottomrule
\end{tabular}
\end{table}

Los valores ausentes se serializan como cadena vac\'ia \texttt{""} en el CSV
(\texttt{agente\_id} vac\'io: ticket sin agente asignado; \texttt{fecha\_resolucion}
vac\'ia: ticket a\'un abierto). pandas convierte \texttt{""} $\to$
\texttt{NaN} y PySpark \texttt{""} $\to$ \texttt{NULL}: ese criterio \'unico
de normalizaci\'on de nulos, aplicado id\'enticamente en ambos motores, es lo
que garantiza la equivalencia num\'erica de T1--T5 verificada en la
Secci\'on~\ref{sec:equivalencia} (criterio 1.4). \texttt{data/raw/} no se
versiona en el repositorio (es regenerable bit a bit con los mismos
argumentos); en su lugar se versionan los diez CSV de salida del pipeline
(\texttt{data/pandas/*.csv}, \texttt{data/spark/*.csv}) como evidencia
directa de la ejecuci\'on.


\subsection{Configuraci\'on efectiva de Spark}
\label{sec:config-spark}

La sesi\'on de Spark se inicializa con \texttt{master=local[N]}, donde $N \in
\{1,2,4\}$ simula el n\'umero de executors en una sola m\'aquina (Google
Colab). El par\'ametro \texttt{spark.executor.instances=4} se fija por
requisito del checklist de la asignatura, pero en modo \texttt{local[N]}
Spark crea siempre \emph{un} proceso executor con $N$ hilos: la
paralelizaci\'on efectiva es la que da \texttt{local[N]}, no el valor de
\texttt{executor.instances}, limitaci\'on que se documenta expl\'icitamente
para que no se interprete err\'oneamente como un cl\'uster de cuatro nodos
f\'isicos. El \emph{broadcast join} se desactiva
(\texttt{autoBroadcastJoinThreshold=-1}) para forzar que T3 ejecute siempre
un \emph{sort-merge join} con \emph{shuffle} real y distribuido; sin esta
configuraci\'on, Spark transmitir\'ia la tabla peque\~na \texttt{agentes} a
todos los executors y el \emph{shuffle} --el fen\'omeno central del
an\'alisis de la Secci\'on~\ref{sec:analisis}-- desaparecer\'ia. La
Tabla~\ref{tab:sparkconf} transcribe, sin modificaciones, el subconjunto
relevante de la salida real de \texttt{spark.sparkContext.getConf().getAll()}
obtenida en la ejecuci\'on documentada en \texttt{notebooks/PE\_U4\_pipeline\_spark.ipynb}.

\begin{table}[htbp]
\centering
\caption{Configuraci\'on efectiva de la SparkSession (salida real de \texttt{getConf().getAll()}, ejecuci\'on N=4).}
\label{tab:sparkconf}
\begin{tabular}{@{}ll@{}}
\toprule
\textbf{Clave} & \textbf{Valor} \\
\midrule
\texttt{spark.master} & \texttt{local[4]} \\
\texttt{spark.app.name} & \texttt{pe-u4-notebook (N=4)} \\
\texttt{spark.executor.instances} & \texttt{4} \\
\texttt{spark.sql.autoBroadcastJoinThreshold} & \texttt{-1} \\
\texttt{spark.sql.shuffle.partitions} & \texttt{16} \\
\texttt{spark.sql.session.timeZone} & \texttt{UTC} \\
\texttt{spark.ui.enabled} & \texttt{true} \\
\texttt{spark.driver.host} & \texttt{127.0.0.1} \\
\bottomrule
\end{tabular}
\end{table}

El n\'umero de particiones de \emph{shuffle} se fija en
\texttt{max(4$\times N$, 8)} (16 para $N=4$), de modo que el paralelismo del
\emph{shuffle} escale acorde al n\'umero de executors simulados en cada
corrida. \texttt{spark.sql.session.timeZone=UTC} asegura que las funciones
\texttt{year()}/\texttt{month()} de Spark extraigan los mismos componentes de
fecha que \texttt{pandas.Timestamp} en T4. \texttt{spark.ui.enabled=true}
habilita la Spark UI en el puerto 4040, capturada como evidencia en la
Figura~\ref{fig:sparkui}.

\subsection{Implementaci\'on con pandas y PySpark}
\label{sec:implementacion}

\begin{sloppypar}
Las cinco transformaciones (T1 filtrado, T2 agrupaci\'on, T3 join, T4
derivadas, T5 top-N) se implementan de forma espejada en los m\'odulos
\texttt{transformaciones\_}\allowbreak\texttt{pandas.py} y
\texttt{transformaciones\_}\allowbreak\texttt{spark.py}, dentro de la carpeta
\texttt{src/}. A continuaci\'on se listan los
fragmentos representativos de cada transformaci\'on; el c\'odigo completo,
incluyendo la lectura y normalizaci\'on de nulos, est\'a disponible en el
repositorio.
\end{sloppypar}

\begin{lstlisting}[language=Python,caption={T1 -- Filtrado (pandas).},label={lst:t1-pandas}]
def t1_filtrado(df: pd.DataFrame) -> pd.DataFrame:
    return base_filtrada(df)[
        ["ticket_id", "cliente_id", "agente_id", "categoria", "prioridad",
         "canal", "zona", "fecha_creacion", "fecha_resolucion"]
    ]
\end{lstlisting}

\begin{lstlisting}[language=Python,caption={T1 -- Filtrado (PySpark).},label={lst:t1-spark}]
def t1_filtrado(df):
    return base_filtrada(df).select(
        "ticket_id", "cliente_id", "agente_id", "categoria", "prioridad",
        "canal", "zona", "fecha_creacion", "fecha_resolucion",
    )
\end{lstlisting}

\begin{lstlisting}[language=Python,caption={T2 -- Agrupaci\'on con 6 agregaciones por (categoria, zona) (pandas).},label={lst:t2-pandas}]
def t2_agrupacion(df: pd.DataFrame) -> pd.DataFrame:
    d = df[df["categoria"].notna() & df["zona"].notna()].copy()
    g = (
        d.groupby(["categoria", "zona"], dropna=False)
        .agg(
            cnt=("ticket_id", "count"),
            sum_sla=("sla_horas", "sum"),
            avg_sla=("sla_horas", "mean"),
            max_hora=("hora_creacion", "max"),
            avg_dia=("dia_semana_creacion", "mean"),
            min_mes=("mes_creacion", "min"),
        )
        .reset_index()
    )
    return g
\end{lstlisting}

\begin{lstlisting}[language=Python,caption={T3 -- Join inner tickets $\bowtie$ agentes, sort-merge con shuffle real (PySpark).},label={lst:t3-spark}]
def t3_join(df, agentes):
    base = base_filtrada(df)
    unido = base.join(agentes, on="agente_id", how="inner")
    unido = unido.withColumnRenamed("nombre", "nombre_agente")
    return unido.orderBy("ticket_id").select(
        "ticket_id", "cliente_id", "agente_id", "categoria", "prioridad",
        "canal", "zona", "fecha_creacion", "nombre_agente", "equipo", "nivel",
    )
\end{lstlisting}

\begin{lstlisting}[language=Python,caption={T4 -- Variables derivadas: fecha, tiempo de respuesta, SLA, prioridad cr\'itica (pandas).},label={lst:t4-pandas}]
def t4_derivada(df: pd.DataFrame) -> pd.DataFrame:
    d = df[df["fecha_creacion"].notna()].copy()
    d["anio"] = d["fecha_creacion"].dt.year
    d["mes"] = d["fecha_creacion"].dt.month
    d["dia"] = d["dia_semana_creacion"]
    d["tiempo_respuesta_h"] = (
        (d["fecha_resolucion"] - d["fecha_creacion"]).dt.total_seconds() / 3600.0
    )
    dentro_sla = d["tiempo_respuesta_h"] <= d["sla_horas"]
    d["sla_cumplido"] = np.where(
        d["fecha_resolucion"].isna(), np.nan, np.where(dentro_sla, 1, 0)
    )
    d["prioridad_critica"] = (d["prioridad"] == "CRITICA").astype(int)
    d["asunto_mayus"] = d["asunto"].str.upper()
    columnas = [
        "ticket_id", "anio", "mes", "dia", "tiempo_respuesta_h",
        "sla_cumplido", "prioridad_critica", "asunto_mayus", "zona",
    ]
    return d[columnas]
\end{lstlisting}

\begin{lstlisting}[language=Python,caption={T5 -- Top-10 asuntos con m\'as tickets (PySpark).},label={lst:t5-spark}]
def t5_topn(df):
    d = df.filter(F.col("asunto").isNotNull())
    return (
        d.groupBy("asunto")
        .count()
        .withColumnRenamed("count", "cnt")
        .orderBy(F.col("cnt").desc(), F.col("asunto").asc())
        .limit(10)
    )
\end{lstlisting}

\subsection{Verificaci\'on de equivalencia pandas $\leftrightarrow$ PySpark}
\label{sec:equivalencia}

Para cada una de las cinco transformaciones se verific\'o, sobre la
ejecuci\'on real del pipeline, la cardinalidad de filas, la correspondencia
de columnas y al menos un agregado de control (sumas, medias y claves de
agrupaci\'on) entre el resultado en pandas y en PySpark. La
Tabla~\ref{tab:equivalencia} resume el resultado: las cinco transformaciones
son num\'ericamente equivalentes entre ambos motores.

\begin{table}[htbp]
\centering
\caption{Verificaci\'on de equivalencia num\'erica pandas $\leftrightarrow$ PySpark.}
\label{tab:equivalencia}
\resizebox{\textwidth}{!}{%
\begin{tabular}{@{}lrrccccc@{}}
\toprule
\textbf{Transf.} & \textbf{Filas pandas} & \textbf{Filas Spark} & \textbf{Columnas} & \textbf{Cardinalidad} & \textbf{Sumas} & \textbf{Medias} & \textbf{Claves} \\
\midrule
T1 & 180\,003 & 180\,003 & S\'i & S\'i & S\'i & S\'i & S\'i \\
T2 & 30       & 30       & S\'i & S\'i & S\'i & S\'i & S\'i \\
T3 & 180\,003 & 180\,003 & S\'i & S\'i & S\'i & S\'i & S\'i \\
T4 & 600\,000 & 600\,000 & S\'i & S\'i & S\'i & S\'i & S\'i \\
T5 & 10       & 10       & S\'i & S\'i & S\'i & S\'i & S\'i \\
\bottomrule
\end{tabular}%
}
\end{table}

\subsection{Versiones declaradas del entorno}
\label{sec:versiones}

La Tabla~\ref{tab:versiones} transcribe, sin modificaciones, la salida real
de \texttt{python src/medicion.py -{}-modo versiones} obtenida durante la
ejecuci\'on documentada en Google Colab (criterio 1.4, nivel 4).

\begin{table}[htbp]
\centering
\caption{Versiones reales del entorno de ejecuci\'on.}
\label{tab:versiones}
\begin{tabular}{@{}ll@{}}
\toprule
\textbf{Componente} & \textbf{Versi\'on} \\
\midrule
Python & 3.12.13 \\
Paquete PySpark & 3.5.3 \\
Motor Spark & 3.5.3 \\
pandas & 2.2.3 \\
numpy & 2.5.1 \\
matplotlib & 3.9.2 \\
faker & 40.36.0 \\
Plataforma & Linux-6.6.122+-x86\_64-with-glibc2.35 (Google Colab) \\
Java & OpenJDK 17.0.19 \\
Fecha de ejecuci\'on (UTC) & 2026-08-07T18:57:00+00:00 \\
\bottomrule
\end{tabular}
\end{table}


\section{Resultados}
\label{sec:resultados}

\subsection{Tiempos de ejecuci\'on: pandas vs.\ PySpark}

La Tabla~\ref{tab:tiempos} resume el tiempo mediano (de cinco repeticiones,
tras un calentamiento descartado) de cada transformaci\'on en ambos motores,
junto con el speedup $S=T_{\text{pandas}}/T_{\text{PySpark}}$. Para T3 se
reporta el resultado con $N=4$ executors (configuraci\'on base de las dem\'as
transformaciones); el escalado completo de T3 con $N=1,2,4$ se presenta en la
Secci\'on~\ref{sec:analisis}.

\begin{table}[htbp]
\centering
\caption{Tiempos medianos (s) y speedup por transformaci\'on.}
\label{tab:tiempos}
\begin{tabular}{@{}lrrrp{5.2cm}@{}}
\toprule
\textbf{Transf.} & \textbf{pandas (\si{\second})} & \textbf{PySpark (\si{\second})} & \textbf{Speedup} & \textbf{Interpretaci\'on} \\
\midrule
T1 & 9.44  & 6.21 & 1.52$\times$ & Filtrado: paralelismo de datos casi puro, sin shuffle. \\
T2 & 8.34  & 4.36 & 1.91$\times$ & Agrupaci\'on con 6 agregados; shuffle acotado (30 grupos de salida). \\
T3 & 10.12 & 7.09 & 1.43$\times$ & Join con shuffle real; menor speedup del grupo (Secci\'on~\ref{sec:analisis}). \\
T4 & 12.30 & 8.38 & 1.47$\times$ & Mayor tiempo absoluto (columnas derivadas por fila, 600\,000 filas). \\
T5 & 6.27  & 2.45 & 2.56$\times$ & Mayor speedup: agregaci\'on simple + \texttt{limit(10)}, salida m\'inima. \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.8\textwidth]{../resultados/figuras/fig1_barras.png}
\caption{Tiempo mediano por transformaci\'on, pandas vs.\ PySpark.}
\label{fig:barras}
\end{figure}

La Figura~\ref{fig:barras} muestra que las cinco transformaciones son m\'as
r\'apidas en PySpark que en pandas bajo el protocolo de medici\'on de este
proyecto, sin excepci\'on. El patr\'on no es uniforme: T5 (top-N) obtiene el
mayor speedup (2.56$\times$) porque es la operaci\'on con menor volumen de
datos de salida (una agregaci\'on simple seguida de \texttt{limit(10)}, que
Catalyst puede optimizar agresivamente), mientras que T3 (join) obtiene el
menor speedup (1.43$\times$) porque es la \'unica transformaci\'on que
requiere \emph{shuffle} completo de las dos tablas para poder emparejarlas
--el costo de comunicaci\'on entre executors reduce, pero no elimina, la
ventaja de paralelizar el c\'omputo.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.8\textwidth]{../resultados/figuras/fig2_speedup.png}
\caption{Speedup medido de T3 para $N=1,2,4$ frente a la curva te\'orica de Amdahl.}
\label{fig:speedup}
\end{figure}

La Figura~\ref{fig:speedup} superpone el speedup medido de T3
($S_{\text{interno}}(N)=T_{\text{Spark}}(1)/T_{\text{Spark}}(N)$) con la
curva te\'orica de Amdahl calculada con la fracci\'on paralelizable estimada
a partir de los propios datos ($p=0.173$). El sistema real queda por debajo
del l\'imite te\'orico para $N=2$ y pr\'acticamente lo alcanza -- sin
superarlo -- para $N=4$, donde de hecho el tiempo absoluto es ligeramente
mayor que en $N=2$: la Secci\'on~\ref{sec:analisis} discute esta
no-monotonicidad como evidencia de sobrecarga de coordinaci\'on que supera el
beneficio marginal de duplicar executors en un cl\'uster local simulado.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.8\textwidth]{../resultados/figuras/fig3_eficiencia.png}
\caption{Eficiencia $E(N)=S(N)/N$ de T3 para $N=1,2,4$.}
\label{fig:eficiencia}
\end{figure}

La Figura~\ref{fig:eficiencia} muestra la ca\'ida caracter\'istica de
eficiencia predicha por la Ley de Amdahl: de $E(1)=1.00$ a $E(2)=0.56$ y
$E(4)=0.28$, una reducci\'on de m\'as de la mitad cada vez que se duplican
los executors --el s\'intoma cuantitativo de que la fracci\'on paralelizable
observada de T3 es baja.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.75\textwidth]{../evidencia/spark_ui_t3.png}
\caption{Spark UI: DAG y stages de la ejecuci\'on de T3 (join), incluyendo la etapa de \emph{shuffle}.}
\label{fig:sparkui}
\end{figure}

La Figura~\ref{fig:sparkui} documenta, como evidencia de ejecuci\'on real
(criterio 1.5), el DAG y los \emph{stages} generados por Spark al ejecutar
T3: se observa la etapa de lectura y filtrado, la etapa de \emph{shuffle}
asociada al join, y la etapa final de escritura que materializa el
resultado.

\section{An\'alisis Cr\'itico}
\label{sec:analisis}

\subsection{Speedup de las cinco transformaciones}

Como se resume en la Tabla~\ref{tab:tiempos}, el speedup
$S=T_{\text{pandas}}/T_{\text{PySpark}}$ obtenido fue: T1 $=1.52\times$, T2
$=1.91\times$, T3 (N=4) $=1.43\times$, T4 $=1.47\times$ y T5 $=2.56\times$.
Las cinco transformaciones muestran speedup positivo (PySpark m\'as r\'apido
que pandas en todos los casos), sin que ninguna caiga por debajo de
$1\times$ bajo este protocolo de medici\'on.

\subsection{Fracci\'on serial observada de T3: sustituci\'on num\'erica}

El escalado de T3 se midi\'o con $N=1,2,4$ executors, obteniendo los tiempos
medianos $T_{\text{Spark}}(1)=7.891465$\,s, $T_{\text{Spark}}(2)=7.058974$\,s
y $T_{\text{Spark}}(4)=7.087362$\,s. El speedup interno (relativo al propio
Spark con $N=1$, la comparaci\'on correcta para aplicar la Ley de Amdahl
dentro del mismo motor) es:
\[
S_{\text{interno}}(2) = \frac{T_{\text{Spark}}(1)}{T_{\text{Spark}}(2)} = \frac{7.891465}{7.058974} = 1.117934,
\qquad
S_{\text{interno}}(4) = \frac{7.891465}{7.087362} = 1.113456.
\]

Sustituyendo en la ecuaci\'on~\eqref{eq:fobs} para $N=2$:
\[
f_{\text{obs}}(2) = \frac{\dfrac{1}{1.117934} - \dfrac{1}{2}}{1 - \dfrac{1}{2}}
= \frac{0.894522 - 0.500000}{0.500000}
= \frac{0.394522}{0.500000}
= 0.789015 .
\]

Y para $N=4$:
\[
f_{\text{obs}}(4) = \frac{\dfrac{1}{1.113456} - \dfrac{1}{4}}{1 - \dfrac{1}{4}}
= \frac{0.898086 - 0.250000}{0.750000}
= \frac{0.648086}{0.750000}
= 0.864114 \approx 0.864140 .
\]
(La diferencia de la \'ultima cifra frente al valor reportado por la
funci\'on \texttt{calcular\_amdahl()} de \texttt{transformaciones\_pandas.py},
0.864140, se debe al redondeo intermedio mostrado aqu\'i con seis decimales;
el valor aut\'oritativo es el calculado en punto flotante de doble
precisi\'on por el script, sin redondeos intermedios.)

Ambas estimaciones son consistentes entre s\'i (0.789 y 0.864): la fracci\'on
serial \emph{observada} de T3 es alta --entre 79\,\% y 86\,\% seg\'un el
$N$-- muy por encima de lo que se esperar\'ia de un join si Amdahl midiera
\'unicamente la fracci\'on algor\'itmicamente secuencial del c\'odigo. Esto
es, en s\'i mismo, el primer hallazgo relevante del an\'alisis: confirma que
$f_{\text{obs}}$ no est\'a midiendo secuencialidad algor\'itmica pura, sino
--como se advirti\'o en la Secci\'on~\ref{sec:fundamento-amdahl}-- una
mezcla de fracci\'on serial real m\'as sobrecarga de planificaci\'on,
serializaci\'on y, sobre todo, el \emph{shuffle} distribuido que redistribuye
las 180\,003 filas de la base filtrada y las 300 filas de \texttt{agentes}
entre particiones antes de poder emparejarlas (Figura~\ref{fig:sparkui}).

Promediando las dos estimaciones se obtiene la fracci\'on serial
representativa usada para el l\'imite te\'orico:
\[
f_{\text{media}} = \frac{0.789015 + 0.864140}{2} = 0.826577,
\qquad
p = 1 - f_{\text{media}} = 0.173423 .
\]

\subsection{Speedup m\'aximo te\'orico ($S_{\max}$)}

Aplicando la ecuaci\'on~\eqref{eq:smax} con la fracci\'on serial media
calculada:
\[
S_{\max} = \frac{1}{f_{\text{media}}} = \frac{1}{0.826577} = 1.209808 .
\]
En t\'erminos pr\'acticos, este n\'umero es el techo absoluto de aceleraci\'on
que ning\'un n\'umero adicional de executors --ni 8, ni 16, ni un cl\'uster
real de cientos de nodos-- podr\'ia superar para T3 bajo esta configuraci\'on
de shuffle: un techo de apenas 1.21$\times$, notablemente bajo, y coherente
con el speedup interno ya observado en $N=4$ (1.113$\times$), que se
encuentra a menos del 10\,\% de distancia relativa de ese l\'imite te\'orico.

\subsection{Fracci\'on serial algor\'itmica vs.\ sobrecarga observada, aplicada a T3}

Como se plante\'o en la Secci\'on~\ref{sec:fundamento-amdahl}, es un error
conceptual tratar $f_{\text{obs}}$ como si fuera la fracci\'on de
c\'odigo intr\'insecamente secuencial del algoritmo de join. El join en s\'i
--emparejar cada ticket con su agente por clave-- es una operaci\'on
altamente paralelizable en principio: cada par (ticket, agente) puede
resolverse de forma independiente si ambas filas ya est\'an en el mismo
executor. Lo que \emph{no} es paralelizable, y por tanto se contabiliza como
parte de $f_{\text{obs}}$, es el \textbf{\emph{shuffle}} previo: la
redistribuci\'on f\'isica de las filas de \texttt{tickets} y
\texttt{agentes} entre executors seg\'un el hash de \texttt{agente\_id},
necesaria porque no hay garant\'ia de que las filas relacionadas empiecen en
el mismo executor. Esa redistribuci\'on implica escritura a disco/red de
datos intermedios, serializaci\'on y sincronizaci\'on entre executors --todo
overhead de coordinaci\'on del motor, no l\'ogica secuencial del algoritmo de
join. Con \texttt{autoBroadcastJoinThreshold=-1} (Secci\'on~\ref{sec:config-spark})
este \emph{shuffle} es obligatorio en todas las corridas, lo cual explica por
qu\'e $f_{\text{obs}}$ es alto pese a que el join no es, conceptualmente, una
operaci\'on serial.

Esta distinci\'on tambi\'en explica el hallazgo m\'as llamativo de la
Figura~\ref{fig:speedup}: el tiempo de T3 en $N=4$ (7.087\,s) es
\emph{ligeramente mayor} que en $N=2$ (7.059\,s), un resultado no mon\'otono
--m\'as executors, tiempo pr\'acticamente igual o peor-- que ser\'ia
sorprendente si la \'unica variable en juego fuera el c\'omputo paralelizable
del join. Con el \emph{shuffle} como cuello de botella dominante, sin
embargo, este resultado es coherente: al pasar de $N=2$ a $N=4$ se duplica el
n\'umero de particiones de \emph{shuffle} (de 8 a 16, seg\'un
\texttt{max(4$\times N$, 8)}), lo cual aumenta la cantidad de archivos
intermedios y de tareas de coordinaci\'on del \emph{shuffle} sin que exista
--en un cl\'uster local de un solo proceso f\'isico, simulado con hilos, y no
un cl\'uster real de nodos separados-- una ganancia proporcional de ancho de
banda de red o de I/O para compensar esa sobrecarga adicional. Es, en suma,
la se\~nal de que el sistema ya alcanz\'o, para esta operaci\'on y este
volumen de datos, el punto de rendimientos decrecientes que predice la propia
Ley de Amdahl con una $p$ tan baja como 0.173.

\subsection{Umbral de rentabilidad de Spark para ACC}

La Tabla~\ref{tab:umbral} resume la mediana de tiempo de T2 (agrupaci\'on)
medida en pandas y PySpark para tama\~nos crecientes del dataset, de 10\,000
a 600\,000 filas.

\begin{table}[htbp]
\centering
\caption{Tiempo mediano (s) de T2 por tama\~no de dataset, pandas vs.\ PySpark.}
\label{tab:umbral}
\begin{tabular}{@{}rrr@{}}
\toprule
\textbf{Filas} & \textbf{pandas (\si{\second})} & \textbf{PySpark (\si{\second})} \\
\midrule
10\,000  & 6.27 & 1.38 \\
50\,000  & 5.98 & 2.94 \\
100\,000 & 8.02 & 4.81 \\
250\,000 & 8.30 & 5.66 \\
500\,000 & 6.60 & 5.41 \\
600\,000 & 6.49 & 4.94 \\
\bottomrule
\end{tabular}
\end{table}

El resultado emp\'irico es que, bajo este protocolo de medici\'on, PySpark ya
es m\'as r\'apido que pandas en el tama\~no m\'as peque\~no probado
(10\,000 filas): no se observa, dentro del rango medido, un punto de cruce
donde pandas sea preferible. Esto \emph{no} significa que Spark no tenga
overhead fijo de arranque --lo tiene, y es sustancial en un entorno real--
sino que ese overhead queda expl\'icitamente fuera de la medici\'on por
dise\~no del protocolo (Secci\'on~\ref{sec:procedimiento}): ``el arranque de
la SparkSession no se cronometra''. Es una distinci\'on metodol\'ogica
importante para no sobreinterpretar el resultado: lo que la Tabla~\ref{tab:umbral}
mide es el \emph{tiempo de procesamiento puro} una vez que la sesi\'on ya
est\'a activa, no la latencia total percibida por un analista que ejecuta un
script desde cero. En un despliegue real de ACC, si cada reporte se ejecuta
como un proceso Spark independiente que debe arrancar su propia sesi\'on
(varios segundos de overhead fijo en un cl\'uster gestionado como EMR o
Dataproc), el umbral de rentabilidad efectivo ser\'ia m\'as alto que lo que
sugiere la Tabla~\ref{tab:umbral} tomada de forma aislada. La recomendaci\'on
razonable para ACC es, por tanto, doble: (a) si los reportes se ejecutan en
una sesi\'on Spark persistente (por ejemplo, un cl\'uster que atiende
m\'ultiples consultas gerenciales durante el d\'ia), la migraci\'on a Spark
se justifica ya desde vol\'umenes moderados, como sugiere esta tabla; (b) si
cada reporte arranca su propia sesi\'on desde cero, conviene reservar Spark
para los reportes sobre el hist\'orico completo (cientos de miles de tickets)
y mantener pandas para consultas puntuales sobre subconjuntos peque\~nos,
donde el overhead fijo de sesi\'on dominar\'ia el tiempo total.

\section{Preguntas de An\'alisis (Anexo A)}
\label{sec:preguntas}

\subsection*{Pregunta 1: Speedup m\'aximo de T3 y $N$ para el 90\,\% del m\'aximo}

Con la fracci\'on paralelizable estimada para T3, $p=0.173423$ (equivalente a
$f_{\text{media}}=0.826577$), el speedup m\'aximo te\'orico ya calculado en
la Secci\'on~\ref{sec:analisis} es $S_{\max}=1/f_{\text{media}}=1/0.826577=1.209808$.
Para encontrar el n\'umero de executors $N$ necesario para alcanzar el
90\,\% de ese l\'imite, se parte de la ecuaci\'on~\eqref{eq:amdahl} y se
impone $S(N)=0.9\,S_{\max}$:
\[
0.9\,S_{\max} = 0.9 \times 1.209808 = 1.088827 .
\]
\[
S(N) = \frac{1}{f_{\text{media}} + \dfrac{p}{N}} = 1.088827
\quad\Longrightarrow\quad
f_{\text{media}} + \frac{p}{N} = \frac{1}{1.088827} = 0.918420 .
\]
Despejando el t\'ermino paralelo:
\[
\frac{p}{N} = 0.918420 - f_{\text{media}} = 0.918420 - 0.826577 = 0.091843 .
\]
Y despejando $N$:
\[
N = \frac{p}{0.091843} = \frac{0.173423}{0.091843} \approx 1.8885 .
\]
Como $N$ debe ser un entero de executors, $N=2$ es el primer valor entero que
alcanza (y de hecho supera levemente) el 90\,\% del speedup m\'aximo
te\'orico. Este resultado matem\'atico es plenamente consistente con el dato
experimental: en la Tabla del an\'alisis cr\'itico, el speedup interno medido
en $N=2$ (1.117934) ya representa el 92.4\,\% de $S_{\max}$
($1.117934/1.209808=0.9241$), y $N=4$ no mejora sustancialmente esa
proporci\'on (1.113456/1.209808=0.9204). En otras palabras: con una
fracci\'on paralelizable tan baja como 17.3\,\%, el sistema satura su
beneficio te\'orico con muy pocos executors, y agregar m\'as recursos de
c\'omputo m\'as all\'a de $N=2$ no tiene, para esta operaci\'on y este
volumen de datos, retorno pr\'actico --exactamente la advertencia central de
la Ley de Amdahl aplicada a un caso real \cite{amdahl1967}.

\subsection*{Pregunta 2: transformaciones con overhead de Spark y tama\~no m\'inimo rentable}

Bajo el protocolo de medici\'on de este proyecto, las cinco transformaciones
obtuvieron speedup superior a $1\times$ (Tabla~\ref{tab:tiempos}), por lo que
no se observ\'o, en el tama\~no fijo de 600\,000 filas, ning\'un caso donde
PySpark resultara m\'as lento que pandas. Esto no significa que el overhead
de Spark sea irrelevante para ACC: significa que, a 600\,000 filas, el
volumen de trabajo por transformaci\'on ya es suficiente para que el
paralelismo compense el costo fijo de coordinaci\'on de tareas, serializaci\'on
JVM$\leftrightarrow$Python (v\'ia Py4J) y planificaci\'on del DAG que tiene
toda ejecuci\'on de Spark, incluso en modo local. El experimento de umbral
(Tabla~\ref{tab:umbral}, Secci\'on~\ref{sec:analisis}) es el que permite
extrapolar hacia tama\~nos menores: en \'el, PySpark ya es m\'as r\'apido que
pandas incluso en el punto m\'as peque\~no medido (10\,000 filas), lo cual
sugiere --dentro de las limitaciones metodol\'ogicas ya discutidas sobre el
arranque de sesi\'on no cronometrado-- que el overhead de \emph{procesamiento}
puro de Spark es bajo para operaciones tan simples como T2. Aplicado a la
decisi\'on de negocio de ACC: si el equipo de TI ejecuta sus reportes de
tickets sobre una sesi\'on Spark ya activa (por ejemplo, un notebook de
an\'alisis diario o un cl\'uster compartido), la migraci\'on se justifica
pr\'acticamente desde cualquier volumen de tickets diarios que ya se procese
en un pipeline automatizado; si en cambio cada reporte dispara su propio
proceso Spark desde cero, el overhead fijo de arranque de sesi\'on (varios
segundos, no medidos en este experimento por dise\~no) solo se amortiza a
partir de vol\'umenes de al menos decenas de miles de tickets por corrida
--un umbral que, para un ISP de tama\~no medio con miles de tickets
mensuales, se alcanza con facilidad en reportes acumulados semanales o
mensuales, aunque no necesariamente en consultas ad-hoc sobre el d\'ia en
curso.

\subsection*{Pregunta 3: Spark Structured Streaming para tickets en tiempo real}

Si los tickets de soporte de ACC llegaran como un flujo continuo de eventos
en vez de procesarse por lotes, la arquitectura del pipeline cambiar\'ia de
forma sustancial usando Spark Structured Streaming. La \textbf{fuente}
(\emph{source}) dejar\'ia de ser un archivo CSV est\'atico y pasar\'ia a ser
un sistema de mensajer\'ia como Apache Kafka, donde cada apertura de ticket
se publicar\'ia como un evento en el momento exacto en que el cliente lo
reporta (por WEB, APP, tel\'efono o email). El \textbf{sink} (destino) ser\'ia
el almacenamiento de los resultados agregados en tiempo casi real: por
ejemplo, una tabla de conteos por categor\'ia y zona actualizada
continuamente (el equivalente en streaming de T2) que alimenta un tablero de
monitoreo operativo, o una tabla de alertas que se activa cuando el conteo de
tickets de una misma zona supera un umbral en una ventana de tiempo corta
--la se\~nal que interesar\'ia a un ISP para detectar un corte masivo de
servicio en curso. La \textbf{pol\'itica de \emph{triggers}} definir\'ia cada
cu\'anto se procesan los micro-lotes acumulados desde la \'ultima ejecuci\'on
(por ejemplo, cada 30 segundos con \texttt{trigger(processingTime=`30
seconds')}), equilibrando la latencia de la alerta con el costo de procesar
lotes demasiado peque\~nos con demasiada frecuencia. Finalmente, las
\textbf{garant\'ias de entrega} determinar\'ian c\'omo se maneja la
posibilidad de que un mismo evento de ticket se procese m\'as de una vez ante
un fallo y reinicio del sistema: \emph{at-least-once} garantiza que ning\'un
ticket se pierda pero permite duplicados (aceptable si la agregaci\'on
posterior es idempotente, por ejemplo contando por \texttt{ticket\_id}
\'unico), mientras que \emph{exactly-once} --que Structured Streaming ofrece
mediante \emph{checkpointing} y fuentes/sumideros transaccionales-- evita
duplicados por completo a costa de mayor complejidad y latencia. Esta
arquitectura conecta directamente con los patrones de la
Secci\'on~\ref{sec:fundamento-nube}: ser\'ia una arquitectura \textbf{Kappa}
si se decide tratar tambi\'en el hist\'orico de tickets como un flujo
reproducible sobre el mismo motor de streaming (simplificando el
mantenimiento a un solo camino de c\'odigo), o una arquitectura
\textbf{Lambda} si se prefiere mantener el pipeline batch actual (este mismo
informe) para los reportes hist\'oricos exactos, y a\~nadir la capa de
streaming \'unicamente para las alertas operativas de baja latencia
\cite{mell2011,zaharia2016}.

\subsection*{Pregunta 4: por qu\'e Spark es m\'as eficiente que MapReduce para procesamiento iterativo}

La ventaja de Spark sobre MapReduce en cargas de trabajo iterativas se
explica por dos decisiones de dise\~no ya desarrolladas en la
Secci\'on~\ref{sec:fundamento}: el modelo de datos en memoria basado en RDD
con \emph{lineage}, y la evaluaci\'on perezosa. MapReduce, como se describi\'o
en la Secci\'on~\ref{sec:fundamento-mapreduce}, materializa --escribe a
disco, en HDFS-- el resultado de cada job antes de que el siguiente job
pueda leerlo; si un mismo conjunto de datos se necesita en varias iteraciones
sucesivas (por ejemplo, un algoritmo que recalcula agregaciones sobre el
mismo subconjunto de tickets en varias pasadas), MapReduce paga el costo
completo de I/O a disco en cada una de esas pasadas, sin excepci\'on. Spark,
en cambio, puede mantener un RDD o DataFrame en memoria entre operaciones
sucesivas: como el \emph{lineage} registra c\'omo reconstruir cada
partici\'on a partir de los datos originales, Spark no necesita la
redundancia f\'isica en disco que MapReduce s\'i requiere para tolerar
fallos, lo que le permite evitar la escritura intermedia sin sacrificar
tolerancia a fallos \cite{zaharia2012}. En segundo lugar, la evaluaci\'on
perezosa permite que el optimizador (Catalyst, sobre DataFrames) vea el
\textbf{plan completo} de transformaciones antes de ejecutar una sola de
ellas, lo que le permite eliminar trabajo redundante, reordenar operaciones
para reducir el volumen de datos que atraviesa cada etapa, y fusionar
transformaciones consecutivas en una sola pasada f\'isica sobre los datos
\cite{zaharia2016}. MapReduce, al ejecutar cada job como una unidad aislada
que no conoce los jobs anteriores ni posteriores en la cadena, no tiene
forma de aplicar esa clase de optimizaci\'on global: cada job se planifica y
ejecuta de forma independiente. En el contexto de este proyecto, esta
diferencia es observable indirectamente en la Secci\'on~\ref{sec:resultados}:
aunque el pipeline de este experimento ejecuta cada transformaci\'on T1--T5
como una unidad independiente (para poder medir cada una por separado), un
pipeline de producci\'on que encadenara varias de estas transformaciones
sobre el mismo DataFrame base se beneficiar\'ia de exactamente esta ventaja
--algo que MapReduce, por dise\~no, no podr\'ia ofrecer sin reescribir cada
etapa como un job separado con su propio costo de I/O.


\section{Conclusiones Individuales}
\label{sec:conclusiones}

\subsection*{Cristhian Daniel Pacheco C\'ardenas}

Implementar el escalado de T3 con $N=1,2,4$ executors me ense\~n\'o algo que
no era evidente antes de medirlo: agregar m\'as executors no solo puede dejar
de ayudar, puede incluso empeorar levemente el tiempo total, como pas\'o
entre $N=2$ (7.06\,s) y $N=4$ (7.09\,s) en nuestra propia medici\'on. Al
principio pens\'e que era ruido de medici\'on, pero al repetir la
protocolo (calentamiento + 5 repeticiones con mediana) el patr\'on se
mantuvo, y entendiendo la configuraci\'on de \texttt{shuffle.partitions=max(4N,8)}
que yo mismo configur\'e en \texttt{crear\_spark()}, qued\'o claro que
duplicar las particiones de \emph{shuffle} de 8 a 16 agrega m\'as
coordinaci\'on de la que un cl\'uster local de una sola m\'aquina puede
compensar con paralelismo real. Aplicado a un despliegue real de ACC, este
aprendizaje se traduce en algo muy concreto: el n\'umero de executors y el
n\'umero de particiones de \emph{shuffle} de un pipeline de tickets no deben
fijarse por intuici\'on ni copiarse de un tutorial gen\'erico, sino ajustarse
emp\'iricamente al volumen real de datos y a la operaci\'on concreta
--porque, como vimos con T3, m\'as recursos de c\'omputo no siempre significan
mejor rendimiento. Mi aporte principal estuvo en la implementaci\'on de las
transformaciones PySpark y el protocolo de medici\'on con
\texttt{time.perf\_counter}, lo que me oblig\'o a entender en detalle por
qu\'e la evaluaci\'on perezosa de Spark exige forzar una acci\'on
(\texttt{write.csv}) para medir tiempo real y no solo la construcci\'on del
plan.

\subsection*{Robinson Rodrigo Cando Moreno}

Calcular la fracci\'on serial observada de T3 con la ecuaci\'on inversa de
Amdahl fue el momento en que la teor\'ia dej\'o de ser abstracta para m\'i:
obtener $p=0.173$ --una fracci\'on paralelizable de apenas 17\,\%-- para una
operaci\'on de join que, a nivel de c\'odigo, parece completamente
paralelizable, me hizo entender de forma muy concreta la advertencia
metodol\'ogica que hab\'iamos le\'ido en la gu\'ia: lo que Amdahl mide no es
secuencialidad de algoritmo, es secuencialidad de \emph{ejecuci\'on}, y en
Spark esa secuencialidad observada est\'a dominada por el \emph{shuffle}, no
por el join en s\'i. Verificar la equivalencia num\'erica de las cinco
transformaciones entre pandas y PySpark (Tabla~\ref{tab:equivalencia}) tambi\'en
me ense\~n\'o algo espec\'ifico sobre reproducibilidad: la \'unica raz\'on por
la que pudimos confiar en comparar los tiempos de ambos motores es que antes
verificamos, fila por fila y agregado por agregado, que ambos produc\'ian
exactamente el mismo resultado --sin esa verificaci\'on previa, cualquier
diferencia de tiempo podr\'ia haber sido, en realidad, una diferencia de qu\'e
se estaba calculando. Para ACC, esto se traduce en que ning\'un an\'alisis de
rendimiento de un futuro pipeline de tickets deber\'ia tomarse en serio sin
antes confirmar que los resultados de ambos motores coinciden num\'ericamente;
de lo contrario, se estar\'ia optimizando la velocidad de un c\'alculo
incorrecto. Mi aporte se concentr\'o en el an\'alisis de Amdahl/Gustafson, la
verificaci\'on de equivalencia y el estudio del umbral de rentabilidad.

\subsection*{Ernesto Gregory Luna Mora}

Mi rol fue mantener el repositorio y la reproducibilidad del proyecto, y el
aprendizaje m\'as concreto que me deja esta pr\'actica no vino del c\'odigo
de Spark sino de algo mucho m\'as operativo: gestionar el versionado de
archivos de resultados de varias decenas de megabytes. Al subir
\texttt{t4\_derivada.csv} (37--39\,MB, la salida m\'as pesada del pipeline
por tener 600\,000 filas completas) descubr\'i que el l\'imite de 25\,MB por
archivo de la interfaz web de GitHub es distinto del l\'imite real de
\texttt{git} por l\'inea de comandos (100\,MB); entender esa diferencia me
ahorr\'o tener que fragmentar artificialmente un archivo de evidencia que en
realidad no necesitaba fragmentarse, con tal de usar el m\'etodo de subida
correcto en vez de forzar el archivo a caber en la herramienta equivocada.
Ese mismo criterio de reproducibilidad --que cualquier persona pueda clonar
el repositorio y reproducir exactamente los mismos resultados-- es el que
aplicamos en todo el proyecto: el dataset se regenera bit a bit con una
semilla fija, las figuras se regeneran con un script en vez de guardarse a
mano, y las diez salidas del pipeline se versionan como evidencia directa de
que la medici\'on ocurri\'o de verdad. Para un sistema real de ACC en
producci\'on, este aprendizaje se traduce en que la infraestructura de datos
--c\'omo se almacenan, versionan y regeneran los resultados intermedios de un
pipeline-- es tan cr\'itica para la confiabilidad del sistema como la
correcci\'on del c\'odigo de transformaci\'on en s\'i.

\section*{Declaraci\'on de Uso de Inteligencia Artificial Generativa}
\addcontentsline{toc}{section}{Declaraci\'on de Uso de Inteligencia Artificial Generativa}
\label{sec:declaracion-ia}

\textbf{Herramienta utilizada:} Claude (Anthropic), modelo Sonnet~5, a
trav\'es de Claude Code (interfaz de l\'inea de comandos).

\textbf{Prop\'osito del uso:} apoyo en la configuraci\'on y versionado del
repositorio (comandos \texttt{git} para inicializar el repositorio local,
conectarlo al remoto y subir archivos de resultados de gran tama\~no;
diagn\'ostico y resoluci\'on del l\'imite de subida de 25\,MB de la interfaz
web de GitHub frente al l\'imite de 100\,MB de \texttt{git} por l\'inea de
comandos); y redacci\'on de un borrador inicial completo del informe LaTeX
--estructura, fundamento te\'orico, integraci\'on de los resultados num\'ericos
reales generados por el propio pipeline del equipo (tiempos, speedups,
an\'alisis de Amdahl/Gustafson, tabla de equivalencia y umbral de
rentabilidad, tomados directamente de \texttt{resultados/tiempos\_resumen.csv}
y \texttt{tablas\_items\_4\_12\_13\_20\_23.txt})-- posteriormente revisado por
el equipo.

\textbf{Secciones asistidas por IA:} portada (redacci\'on de la
justificaci\'on t\'ecnica), resumen, introducci\'on, las cuatro subsecciones
del fundamento te\'orico (Secci\'on~\ref{sec:fundamento}), el procedimiento
aplicado (Secci\'on~\ref{sec:procedimiento}), los resultados y su
interpretaci\'on (Secci\'on~\ref{sec:resultados}), el an\'alisis cr\'itico con
la sustituci\'on num\'erica de la fracci\'on serial
(Secci\'on~\ref{sec:analisis}), las cuatro preguntas del Anexo A
(Secci\'on~\ref{sec:preguntas}) y un primer borrador de las conclusiones
individuales (Secci\'on~\ref{sec:conclusiones}), que cada integrante debe
revisar, personalizar y validar como reflejo genuino de su propio aprendizaje
antes de la entrega final. Los datos num\'ericos citados en todo el documento
provienen \'integramente de archivos generados por la ejecuci\'on real del
pipeline del equipo (\texttt{resultados/tiempos\_resumen.csv},
\texttt{tablas\_items\_4\_12\_13\_20\_23.txt}, y la salida capturada del
notebook ejecutado); ninguna cifra experimental fue generada o estimada por
la IA.

\newpage
\bibliographystyle{IEEEtran}
\bibliography{references_U4}

\end{document}



